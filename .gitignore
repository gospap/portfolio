# Zola build output
public/

# Left over from the Next.js version, which still lives on main
node_modules
.next
next-env.d.ts
*.tsbuildinfo

.vercel
.env*.local
.DS_Store

# Zola writes resized images here during builds
static/processed_images/
